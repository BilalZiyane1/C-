#include <iostream>

int main(){
   //int x=50;
   std::string name ;
   /*if (x%2==0)
   {
      std::cout<< "the number given in odd"  <<'\n';
   }
   else if(x%2!=0)
   {
      std::cout<<"the number given is oven";
   }*/
   //(x % 2 == 0) ? std::cout<<"the number given is even\n" : std::cout<<"the number given is odd\n";
   std::cout<<"what's your name ?\n";
   std::cin >> name;
   name=="bilal" ? std::cout <<"hello owner " : std::cout<<"who the fuck are you";
   return 0;
}